You are an expert Python spatial‑statistics engineer.

## Context
* **Analysis type:** {{ analysis_type }}
* **First rows of analysis_input.json:**
{{ preview_json }}

## Task
Write a single, fully executable Python 3.11 script that answers the user’s question using the "{{ analysis_type }}" method.
1. Loads the file `results/analysis_input.json`, which contains a flat list of dictionaries (JSON array).  
   Example structure:
   ```json
   [
     { "feature_X": 392000.0, "feature_Y": 2037000.0, "feature_Category": "hut" },
     { "feature_X": 391800.0, "feature_Y": 2036500.0, "feature_Category": "tumulus" }
   ]
   ```
2. Converts the data into a Pandas or GeoPandas DataFrame using `x_column` and `y_column` from the data as spatial coordinates.
3. Performs the spatial analysis.
4. Saves all analysis results into the folder `results/visualisierung/{{ analysis_type }}/`:
   - Save spatial results as a `.geojson` file using `GeoDataFrame.to_file(..., driver="GeoJSON")`
   - Save summary statistics (e.g., Moran's I, p-value, number of islands) as a `.json` file using `json.dump(...)`
   - Use `Path(...).mkdir(parents=True, exist_ok=True)` to ensure the output directory exists
5. Prints a short human-readable interpretation of the result to STDOUT.

## Rules
1. Use only open-source libraries already present in the environment:  
   `pandas`, `geopandas`, `numpy`, `scipy`, `libpysal`, `matplotlib`.
2. If spatial weights (e.g., `DistanceBand`) are used:
   - Exclude isolated points using `weights.islands`.
   - Remove these rows from the GeoDataFrame using `.drop(index=weights.islands)`.
   - Then re-calculate the spatial weights using `DistanceBand.from_dataframe(...)` on the filtered DataFrame.
   - Do **not** use `.subset(...)` – that method does not exist on `DistanceBand`.
   - Log the number of excluded points (`n_islands`), their percentage (`pct_islands`), and their IDs.
   - If all points are isolated, print a warning and exit cleanly.
   - Use crs="EPSG:32636" for the GeoDataFrame
3. If the analysis requires a binary indicator (e.g., group A vs. B), create it from `feature_Category`.
4. Catch and log all exceptions clearly. Do not crash.
5. All distances are interpreted in metres. Default threshold = 5000.
6. ignore NaN, Null or empty values.




### Output format

Return **nothing but** the complete Python script,  
wrapped in a single Markdown code fence:

```python
# >>> BEGIN SCRIPT
...
# <<< END SCRIPT
```

